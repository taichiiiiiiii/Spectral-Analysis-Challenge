# Spectral Analysis Challenge - CLAUDE.md

## プロジェクト概要

詳細は `README.md` を参照。

- **現在のベストスコア**: RMSE = 17.03
- **ブランチ**: `develop`（メイン開発）

## 言語

日本語で応答すること。

## サブエージェント運用ルール

**全てのタスクはサブエージェントに委任して実行する。** メインエージェントはタスク管理・進捗監視・結果統合に徹する。

### サブエージェントの種類と使い分け

#### カスタムエージェント（`.claude/agents/`）

| エージェント | 用途 |
|---|---|
| **implementer** | TDDでコード実装・テスト作成（uv + pytest） |
| **reviewer** | コードレビュー・品質チェック |
| **experimenter** | 実験実行・RMSE収集・ベースライン比較 |
| **submitter** | 提出ファイル生成・バリデーション |
| **researcher** | WebSearch/WebFetchでWeb検索・論文調査 |
| **github-manager** | Issue/PR管理・コメント作成・ブランチ運用（gh CLI） |

#### 組み込みエージェント

| subagent_type | 用途 |
|---|---|
| `Explore` | コードベース調査、ファイル検索、構造把握 |
| `Plan` | 実装計画の策定、アーキテクチャ設計 |

### ワークフロー

1. **タスク受領** → TodoWriteでタスクを分解・登録
2. **調査フェーズ** → Explore / researcher で情報収集（並列実行）
3. **計画フェーズ** → Plan で実装計画を策定
4. **実装フェーズ** → implementer でTDD実装（テスト先行）
5. **レビューフェーズ** → reviewer でコードレビュー・品質確認
6. **実験フェーズ** → experimenter でLOSO-CV実行・ベースライン比較
7. **提出フェーズ** → submitter で提出ファイル生成・バリデーション
8. **GitHub管理** → github-manager でIssue/PRコメント・管理
9. **報告** → ユーザーに結果を簡潔に報告

### サブエージェント運用の原則

- **独立したタスクは並列実行**する（例：調査と既存コードの確認を同時に行う）
- サブエージェントへの指示は**具体的かつ完全**にする（ファイルパス、期待する出力形式を明示）
- サブエージェントの結果を受けて**メインエージェントが判断・統合**する
- 実装後は必ず**reviewerでレビュー**し、問題なければ**experimenterで実験**する
- worktreeの`isolation: "worktree"`は大きな変更時に使用する

### 各フェーズ完了時のレビュー必須ルール

**調査・計画・実装・検証の各フェーズが完了した時点で、必ずレビューを行うこと。**

- サブエージェントの出力をそのまま報告しない。Reviewサブエージェントで内容を精査する
- レビュー観点：
  - **正確性**: 数値・コード・結論が正しいか
  - **公平性**: 比較実験の条件が統一されているか
  - **CLAUDE.mdルール準拠**: 技術的制約・コーディング規約が守られているか
  - **バグ・エッジケース**: 未処理のエラーや境界条件がないか
- 問題が見つかった場合は修正し、再度レビューする
- **問題がなくなるまで次のフェーズに進まない**

### Issue・コメントのレビュー必須ルール

**Issue作成・コメント投稿の前に必ずレビューを行い、問題がなくなるまでレビューを繰り返すこと。**

- 投稿前にメインエージェントが内容を精査する（サブエージェントに丸投げしない）
- レビュー観点：
  - **数値の正確性**: RMSE等のスコアが実験結果と一致しているか
  - **根拠の妥当性**: 期待効果に過度な楽観がないか、未検証の条件を断定していないか
  - **前提条件の明示**: 文献実績の条件（ドメインシフトの有無等）が明記されているか
  - **論理の一貫性**: 結論が分析結果と矛盾していないか
- 問題が見つかった場合は修正し、再度レビューする
- **問題がなくなるまで投稿しない**

## プロジェクト構成

```
src/
  eda/              # EDA分析モジュール
  preprocessing/    # スペクトル前処理
  modeling/         # モデリング
  feature_engineering/  # 特徴量エンジニアリング
scripts/            # 実行スクリプト（run_*）
tests/              # ユニットテスト
Input_data/         # train.csv, test.csv, sample_submit.csv
outputs/            # 可視化・分析結果
```

## 技術スタック

- Python 3.11+ / uv（パッケージ管理）
- scikit-learn, scipy, numpy, pandas
- pytest（テスト）

## コーディング規約

- Issue番号付きのファイル命名: `issue{N}_description.py`
- テストファイル: `test_issue{N}_description.py`
- 実行スクリプト: `run_issue{N}.py`
- テスト実行: `uv run pytest tests/ -v`

## モデル学習時のルール

- **LightGBM/XGBoost等の反復学習モデルでは、学習曲線（training/validation lossの推移）を必ず出力・可視化すること**
- 学習曲線から過学習の有無・early stoppingの妥当性を確認する
- 可視化は `outputs/modeling/` に保存する

## 重要な技術的制約

### ドメインシフト
- trainとtestで樹種が完全に異なる → **樹種固有の特徴に過学習しない**こと
- train（13種）とtest（6種）で樹種が重複しない → 未知樹種への汎化が必須
- 水分吸収帯（約5,200 cm⁻¹, 6,900 cm⁻¹）が予測の鍵

### 目的変数変換
- **PLSでは目的変数のlog変換を使わない**（高成分数でRMSEが発散する傾向をEDAで確認済み）
- ML/DLモデルではlog1p変換 → 予測後にexpm1逆変換

### 前処理・データリーケージ防止
- MSCのリファレンス（平均スペクトル）はtrainのみで計算し、testに適用する
- PCA/StandardScaler等はCV各foldのtrainのみでfit → testはtransformのみ
- TCA（Transfer Component Analysis）はドメイン適応手法のため、ソース・ターゲット両方の特徴量を使用する（ラベルは使用しない教師なし設定）

### モデル学習
- 1,555次元を直接入力すると過学習しやすい → PLS/PCAスコアを特徴量として使用
- LightGBM/XGBoostにはearly stopping（patience=50）を必ず設定する
- Optunaでのハイパーパラメータ探索は、モデルのHP数に応じて試行回数を調整する（HP 1〜2個: 10-15回、3-4個: 20-25回、8-9個: 50回）

### 検証戦略
- LOSO-CV（Leave-One-Species-Out）を使用
- ベイスギfoldは含水率が外挿領域のため参考値として扱う
- サンプル重み付け: 不均衡比率3.6倍（ホワイトオーク51件 vs トチ183件）→ ML訓練時に重み付けを検討
